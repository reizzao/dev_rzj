# iMPORTS MAIN : Importando para exportar ao init app

if [ -f $HOME/$FOLDER_DEV/mybash/bash/.aliases  ]; then
    . $HOME/$FOLDER_DEV/mybash/bash/.aliases
fi

if [ -f $HOME/$FOLDER_DEV/mybash/bash/.vars  ]; then
    . $HOME/$FOLDER_DEV/mybash/bash/.vars
fi

if [ -f $HOME/$FOLDER_DEV/mybash/bash/.imports_module  ]; then
    . $HOME/$FOLDER_DEV/mybash/bash/.imports_module
fi


test_mainmodule() {
  echo "Olá mainmodule 0 !"
}
