# Modules by Github

oigit() {
  echo  "oi Git 1  "
}

gs() {
  git status
}

gl() {
  git reflog
}

grr2() {
  # recuperar att no remoto para o local antes de mexer no local

  # git fetch && git reset --hard && git push && git merge --force # todo: melhorar

  git pull && git push && git merge # antigo ok : recuperar att no remoto para o local antes de mexer no local

}

grr() {
  git pull && git push && git merge
}

ga() {
  git add .
}

gc() {
  git commit -m "$1"
  # todo: no  git reflog não esta mostrando toda mensagem
}

gp() {
  git push
}

parse_git_branch() {
# show branch github in use in terminal bash
     git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/ (\1)/'
}
